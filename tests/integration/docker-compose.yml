services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    ports:
      - "8123:8123"
    volumes:
      - ./ha_config:/config
      - ../../custom_components:/config/custom_components:ro
    environment:
      - TZ=America/New_York
    tmpfs:
      - /tmp
